############################
# PHPDocker.io Redis image #
############################
# Removing our own installation as Redis already provides with an up to date image on alpine
# This image is to avoid breaking people's setups which reference phpdockerio/redis, if you're reading this Dockerfile
# then know you can (and should) swap to redis:alpine directly

FROM redis:alpine

MAINTAINER PHPDocker.io
